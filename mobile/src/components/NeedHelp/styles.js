import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    textRed: {
        color: '#ff0505'
    },

    textHelp: {
        fontSize: 16,
        marginBottom: 10,
    }

});

export default styles;